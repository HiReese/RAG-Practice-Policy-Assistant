#version: '3.8'
services:
  # = docker run -d -p 6333:6333 -v ./qdrant_storage:/qdrant/storage qdrant/qdrant:v1.19.0
  # Access web UI at http://localhost:6333/dashboard
  qdrant:
    image: qdrant/qdrant:v1.19.0
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_storage:/qdrant/storage